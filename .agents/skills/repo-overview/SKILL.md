# Admin-Panel — Repo Overview

## Repository Structure

This is a new/empty repository with only a `README.md` at the root. No application code, build system, or dependencies have been added yet.

```
.
├── README.md
```

## Git Workflow

- **Default branch:** `main`
- **Branch naming convention:** `devin/<timestamp>-<descriptive-slug>` (e.g., `devin/1767730526-dummy-test-change`)
- No pre-commit hooks, CI/CD pipelines, or GitHub Actions are configured.
- No `.pre-commit-config.yaml`, `.husky/`, or similar hook configurations exist.

## Build / Dev / Test / Lint

- No `package.json`, `pyproject.toml`, `Cargo.toml`, `Makefile`, or any other build/dependency configuration exists yet.
- No test framework, linter, or formatter is configured.
- When the project is set up in the future, update this skill with the relevant commands.

## Notes

- The repo does not yet have an application framework chosen. Check back for updates once scaffolding is added.
- No secrets or environment variables are required at this time.
